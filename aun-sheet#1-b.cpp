#include <bits/stdc++.h>
#define ll long long
using namespace std;
 
void fastio () {
    ios_base::sync_with_stdio(false);
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
}

int main () {
    fastio();

    int i;
    long long l;
    char c;
    float f;
    double d;

    cin >> i >> l >> c >> f >> d;
    cout << i << '\n' << l << '\n' << c << '\n' << f << '\n' << d;

    return 0;
}